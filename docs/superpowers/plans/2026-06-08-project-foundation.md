# Project Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Initialize the PropTrack web application foundation so later product modules can be built within the agreed harness, architecture, and verification boundaries.

**Architecture:** Build a Next.js App Router app with TypeScript and Tailwind CSS, then add the folder structure from `ARCHITECTURE.md`. This plan does not implement product modules; it creates the codebase foundation, routes, Supabase client boundaries, environment template, and verification scripts.

**Tech Stack:** Next.js App Router, TypeScript, Tailwind CSS, Supabase, Vercel-compatible environment variables.

---

## Preconditions

- Work from repository root: `C:\Users\Jerence\OneDrive - Monash University\Documents\LandlordApp`.
- Read `AGENTS.md`, `CONTEXT.md`, `PRODUCT_PLAN.md`, `ARCHITECTURE.md`, `UI_DESIGN.md`, and `TODO.md` before executing.
- Preserve existing planning documents.
- If a command fails due to missing network/dependency access, request approval and rerun the exact command with escalation.

## File Structure To Create

Expected app structure after this plan:

```text
app/
├── (auth)/
│   ├── login/page.tsx
│   └── callback/route.ts
├── (app)/
│   ├── dashboard/page.tsx
│   ├── properties/page.tsx
│   ├── properties/[propertyId]/page.tsx
│   ├── vendors/page.tsx
│   ├── reports/page.tsx
│   ├── activity/page.tsx
│   ├── settings/page.tsx
│   └── layout.tsx
├── api/health/route.ts
├── globals.css
├── layout.tsx
└── page.tsx
components/
├── layout/AppShell.tsx
└── ui/EmptyState.tsx
features/
├── portfolios/
├── properties/
├── tenants/
├── agreements/
├── rent-records/
├── expenses/
├── maintenance/
├── vendors/
├── deposits/
├── reminders/
├── activity/
├── reports/
└── sharing/
lib/
├── supabase/client.ts
├── supabase/server.ts
├── auth/require-user.ts
├── validation/result.ts
└── utils/currency.ts
server/
├── actions/
├── queries/
├── services/
└── permissions/
db/
├── migrations/
├── seeds/
└── types/
types/
tests/
```

## Task 1: Initialize Next.js App

**Files:**
- Create: Next.js project files generated by `create-next-app`.
- Preserve: existing markdown harness files.

- [ ] **Step 1: Confirm the repository has no existing app files**

Run:

```powershell
Get-ChildItem -Force
```

Expected:

```text
Existing planning files are present, but no package.json, app/, src/, or next.config.* exists.
```

- [ ] **Step 2: Initialize Next.js in the current directory**

Run:

```powershell
npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir false --import-alias "@/*"
```

Expected:

```text
Next.js app created successfully.
```

If the tool asks whether to overwrite existing files, do not overwrite planning docs. Stop and choose a non-destructive path.

- [ ] **Step 3: Verify generated scripts**

Run:

```powershell
npm run build
```

Expected:

```text
Compiled successfully
```

- [ ] **Step 4: Update TODO.md**

Modify `TODO.md`:

```markdown
- [x] Step 1.1: Initialize the Next.js App Router project with TypeScript.
```

- [ ] **Step 5: Commit foundation initialization**

Run:

```powershell
git add .
git commit -m "chore: initialize nextjs foundation"
```

Expected:

```text
[branch commit] chore: initialize nextjs foundation
```

## Task 2: Create Route Skeleton

**Files:**
- Create: `app/(auth)/login/page.tsx`
- Create: `app/(auth)/callback/route.ts`
- Create: `app/(app)/layout.tsx`
- Create: `app/(app)/dashboard/page.tsx`
- Create: `app/(app)/properties/page.tsx`
- Create: `app/(app)/properties/[propertyId]/page.tsx`
- Create: `app/(app)/vendors/page.tsx`
- Create: `app/(app)/reports/page.tsx`
- Create: `app/(app)/activity/page.tsx`
- Create: `app/(app)/settings/page.tsx`
- Create: `app/api/health/route.ts`
- Modify: `app/page.tsx`

- [ ] **Step 1: Create placeholder pages**

Create each page with this pattern, changing the title per route:

```tsx
export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
    </main>
  );
}
```

Required page titles:

```text
Login
Dashboard
Properties
Property Workspace
Vendors
Reports
Activity
Settings
```

- [ ] **Step 2: Create auth callback route**

Create `app/(auth)/callback/route.ts`:

```ts
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.redirect(new URL("/dashboard", "http://localhost:3000"));
}
```

This is a placeholder only. Supabase callback exchange is implemented later.

- [ ] **Step 3: Create health endpoint**

Create `app/api/health/route.ts`:

```ts
import { NextResponse } from "next/server";

export function GET() {
  return NextResponse.json({ status: "ok" });
}
```

- [ ] **Step 4: Redirect root to dashboard**

Replace `app/page.tsx` with:

```tsx
import { redirect } from "next/navigation";

export default function HomePage() {
  redirect("/dashboard");
}
```

- [ ] **Step 5: Run build**

Run:

```powershell
npm run build
```

Expected:

```text
Compiled successfully
```

- [ ] **Step 6: Update TODO.md**

Mark:

```markdown
- [x] Step 1.3: Create the route groups `(auth)` and `(app)`.
- [x] Step 1.4: Create top-level app routes: dashboard, properties, vendors, reports, activity, settings.
- [x] Step 1.5: Create the property workspace route at `properties/[propertyId]`.
```

- [ ] **Step 7: Commit route skeleton**

Run:

```powershell
git add .
git commit -m "chore: add app route skeleton"
```

## Task 3: Add Layout Components

**Files:**
- Create: `components/layout/AppShell.tsx`
- Create: `components/ui/EmptyState.tsx`
- Modify: `app/(app)/layout.tsx`

- [ ] **Step 1: Create AppShell**

Create `components/layout/AppShell.tsx`:

```tsx
import Link from "next/link";
import type { ReactNode } from "react";

const navItems = [
  { href: "/dashboard", label: "Dashboard" },
  { href: "/properties", label: "Properties" },
  { href: "/vendors", label: "Vendors" },
  { href: "/reports", label: "Reports" },
  { href: "/activity", label: "Activity" },
  { href: "/settings", label: "Settings" },
];

type AppShellProps = {
  children: ReactNode;
};

export function AppShell({ children }: AppShellProps) {
  return (
    <div className="min-h-screen bg-white text-[#0e0f0c]">
      <aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-[rgba(14,15,12,0.12)] bg-white px-4 py-6 md:block">
        <div className="mb-8 text-lg font-semibold">PropTrack</div>
        <nav className="space-y-1">
          {navItems.map((item) => (
            <Link
              className="block rounded-md px-3 py-2 text-sm font-medium text-[#454745] hover:bg-[rgba(22,51,0,0.08)] hover:text-[#163300]"
              href={item.href}
              key={item.href}
            >
              {item.label}
            </Link>
          ))}
        </nav>
      </aside>
      <div className="md:pl-64">
        <header className="border-b border-[rgba(14,15,12,0.12)] bg-white px-4 py-4 md:hidden">
          <div className="text-lg font-semibold">PropTrack</div>
        </header>
        <main className="mx-auto max-w-7xl px-4 py-6">{children}</main>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Create EmptyState**

Create `components/ui/EmptyState.tsx`:

```tsx
type EmptyStateProps = {
  title: string;
  description: string;
};

export function EmptyState({ title, description }: EmptyStateProps) {
  return (
    <section className="rounded-md bg-[rgba(22,51,0,0.08)] p-6">
      <h2 className="text-base font-semibold text-[#0e0f0c]">{title}</h2>
      <p className="mt-2 text-sm text-[#454745]">{description}</p>
    </section>
  );
}
```

- [ ] **Step 3: Apply AppShell layout**

Create or replace `app/(app)/layout.tsx`:

```tsx
import type { ReactNode } from "react";
import { AppShell } from "@/components/layout/AppShell";

type AppLayoutProps = {
  children: ReactNode;
};

export default function AppLayout({ children }: AppLayoutProps) {
  return <AppShell>{children}</AppShell>;
}
```

- [ ] **Step 4: Run build**

Run:

```powershell
npm run build
```

Expected:

```text
Compiled successfully
```

- [ ] **Step 5: Commit layout**

Run:

```powershell
git add .
git commit -m "chore: add app shell layout"
```

## Task 4: Add Architecture Folders And Utilities

**Files:**
- Create directories listed in `ARCHITECTURE.md`.
- Create: `lib/utils/currency.ts`
- Create: `lib/validation/result.ts`
- Create: `.env.example`

- [ ] **Step 1: Create empty feature and server folders**

Run:

```powershell
New-Item -ItemType Directory -Force -Path features\portfolios,features\properties,features\tenants,features\agreements,features\rent-records,features\expenses,features\maintenance,features\vendors,features\deposits,features\reminders,features\activity,features\reports,features\sharing,server\actions,server\queries,server\services,server\permissions,db\migrations,db\seeds,db\types,types,tests,lib\supabase,lib\auth,lib\validation,lib\utils | Out-Null
```

- [ ] **Step 2: Create currency utility**

Create `lib/utils/currency.ts`:

```ts
export function formatMyr(amountCents: number) {
  return new Intl.NumberFormat("ms-MY", {
    currency: "MYR",
    style: "currency",
  }).format(amountCents / 100);
}
```

- [ ] **Step 3: Create action result type**

Create `lib/validation/result.ts`:

```ts
export type ActionResult<T> =
  | { ok: true; data: T }
  | { ok: false; error: string };
```

- [ ] **Step 4: Create environment template**

Create `.env.example`:

```text
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

- [ ] **Step 5: Run build**

Run:

```powershell
npm run build
```

Expected:

```text
Compiled successfully
```

- [ ] **Step 6: Update TODO.md**

Mark:

```markdown
- [x] Step 1.6: Add the folder structure from `ARCHITECTURE.md`.
- [x] Step 1.8: Add environment variable template.
```

- [ ] **Step 7: Commit architecture folders**

Run:

```powershell
git add .
git commit -m "chore: add architecture folders"
```

## Task 5: Add Supabase Client Boundaries

**Files:**
- Modify: `package.json`
- Create: `lib/supabase/client.ts`
- Create: `lib/supabase/server.ts`

- [ ] **Step 1: Install Supabase packages**

Run:

```powershell
npm install @supabase/supabase-js @supabase/ssr
```

Expected:

```text
added ... packages
```

- [ ] **Step 2: Create browser Supabase client**

Create `lib/supabase/client.ts`:

```ts
import { createBrowserClient } from "@supabase/ssr";

export function createClient() {
  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;

  if (!supabaseUrl || !supabaseAnonKey) {
    throw new Error("Missing public Supabase environment variables.");
  }

  return createBrowserClient(supabaseUrl, supabaseAnonKey);
}
```

- [ ] **Step 3: Create server Supabase client**

Create `lib/supabase/server.ts`:

```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";

export async function createClient() {
  const cookieStore = await cookies();
  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;

  if (!supabaseUrl || !supabaseAnonKey) {
    throw new Error("Missing public Supabase environment variables.");
  }

  return createServerClient(supabaseUrl, supabaseAnonKey, {
    cookies: {
      getAll() {
        return cookieStore.getAll();
      },
      setAll(cookiesToSet) {
        try {
          cookiesToSet.forEach(({ name, value, options }) => {
            cookieStore.set(name, value, options);
          });
        } catch {
          // Server Components cannot set cookies. Middleware/auth routes will handle session refresh.
        }
      },
    },
  });
}
```

- [ ] **Step 4: Run build**

Run:

```powershell
npm run build
```

Expected:

```text
Compiled successfully
```

- [ ] **Step 5: Update TODO.md**

Mark:

```markdown
- [x] Step 2.1: Configure Supabase client helpers.
```

- [ ] **Step 6: Commit Supabase boundary**

Run:

```powershell
git add .
git commit -m "chore: add supabase client boundaries"
```

## Self-Review Checklist

- [ ] The route skeleton matches `PRODUCT_PLAN.md` navigation.
- [ ] The folder structure matches `ARCHITECTURE.md`.
- [ ] No product modules are implemented prematurely.
- [ ] `npm run build` passes after each task.
- [ ] `TODO.md` is updated as completed steps finish.
- [ ] Existing planning docs are preserved.
